#include <stdio.h>

/**
*main - prints the size of various types
*Return: 0 if existed properly, non-zero if otherwise
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
